defmodule Foodmap.Repo.Migrations.Newthings do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    drop constraint(:place_users, "place_users_follower_id_fkey")

    alter table(:place_users) do
      modify :follower_id,
             references(:places,
               column: :id,
               name: "place_users_follower_id_fkey",
               type: :uuid,
               prefix: "public",
               on_delete: :delete_all
             )
    end
  end

  def down do
    drop constraint(:place_users, "place_users_follower_id_fkey")

    alter table(:place_users) do
      modify :follower_id,
             references(:places,
               column: :id,
               name: "place_users_follower_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end
  end
end
